/**
 * This tests only the integration of the Argon2 library with Typescript/Javascript, but not the Argon2 library itself.
 * 
 */

describe("true", () => {
    it("should be true", () => {
      expect(true).toBe(true);
    });
  });
  